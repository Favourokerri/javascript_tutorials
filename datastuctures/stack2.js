// Using var
if (true) {
    var variableWithVar = 10;
  }
  
  console.log(variableWithVar); // Accessible outside the block
  
  // Using let
  if (true) {
    let variableWithLet = 20;
  }
  
  //console.log(variableWithLet); // Error! Not accessible outside the block
  